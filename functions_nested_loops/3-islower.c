#include "main.h"
/**
* int_islower - check the code
*
* Return: Always 0.
*/

int _islower(int c);
{
int i;

if (i >= 97) && (i <= 122)
	return (1);
else 
	return (0);
{
_putchar(i);
}
_putchar('\n');
}
