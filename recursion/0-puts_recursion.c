#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts_recursion - check the code.
 * @s: pointer
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	int n;

	for (*s != '\0'; s++)
	{
		return;
	}
	return (0);
}
